# 本地验证清单

所有循环共用此文件。每轮 DS 完成后，agent 会在此追加**该次循环**对应 story 的本地验证项；完成验证后请将对应条目的 **已验证** 改为 `是`。

**条目顺序**：按 story 编号顺序排列（3-2 → 3-3 → 3-4 → 4-1 → 4-2 → 4-3 …）；新增时插入到正确位置。

---

## 2-4-batch-import-products（循环完成日：2026-03-10）

**已验证**：否

1. 登录后使用**电脑浏览器**（或浏览器窗口宽度 ≥768px）访问商品列表 `/products`，应看到「批量录入」按钮（与「新增商品」按钮一起）。
2. 点击「批量录入」或直接访问 `/products/batch`，应进入批量录入页，显示表格（表头：名称、规格、售价、进价），至少一行可编辑，有「添加一行」「批量保存」按钮。
3. 在表格中填写多行商品（名称必填，规格/售价/进价可空），点击「批量保存」，应 Toast「成功录入 N 件商品」并跳转回商品列表，列表中能看到新录入的商品。
4. 再次进入批量录入页，任一行留空「名称」点击「批量保存」，该行应高亮（如红色背景），Toast 提示「第 X 行：请填写商品名称」，且不发起保存请求（列表无新增）。
5. 使用**手机浏览器**或将窗口缩小至窄屏（<768px）访问 `/products/batch`，应自动跳转到 `/products/new`（新增商品页）并 Toast「批量录入请使用电脑端」；在窄屏下商品列表页不显示「批量录入」按钮。

---

## 3-1-customer-entry-list（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/customers`，应被重定向到 `/login?redirect=/customers`。
2. 登录后直接访问 `/customers`，应进入客户列表页，顶部标题为「客户」；无数据时显示「暂无客户，点击下方按钮新增」及「新增客户」按钮。
3. 点击「新增客户」或访问 `/customers/new`，应显示表单：客户姓名（必填）、电话、地址；姓名为空点击「保存」应 Toast「请填写客户姓名」。
4. 填写姓名后保存，应 Toast「保存成功」并跳回客户列表，新客户出现在列表中（显示姓名、电话）。
5. 在列表页搜索框输入姓名或电话关键字（防抖约 300ms），列表应过滤为包含该关键字的客户；清空搜索应恢复全部列表。
6. 列表页下拉刷新，应重新拉取数据；无匹配时显示「没有找到相关客户」。

---

## 3-2-customer-edit-delete（循环完成日：2026-03-10）

**已验证**：否

1. 登录后进入客户列表（如直接访问 `/customers`），点击某条客户应进入详情页（`/customers/:id`），页面显示该客户的姓名、电话、地址，且各字段可编辑。
2. 在详情页修改任一项后点击「保存」，应 Toast「保存成功」；将姓名清空后点保存应提示「请填写客户姓名」。
3. 点击「删除客户」应弹出确认弹窗「确定要删除该客户吗？」；点取消应关闭弹窗且不删除；点确认后应删除该客户、跳回客户列表并 Toast「已删除」。

---

## 3-3-supplier-entry-list（循环完成日：2026-03-10）

**已验证**：否

1. 登录后直接访问 `/suppliers`，应进入供应商列表页，顶部标题为「供应商」；无数据时显示「暂无供应商，点击下方按钮新增」及「新增供应商」按钮。
2. 点击「新增供应商」或访问 `/suppliers/new`，应显示表单：供应商名称（必填）、联系人、电话、主营品类；名称为空点击「保存」应 Toast「请填写供应商名称」。
3. 填写名称后保存，应 Toast「保存成功」并跳回供应商列表，新供应商出现在列表中（显示名称、联系人/电话）。
4. 在列表页搜索框输入名称或联系人关键字（防抖约 300ms），列表应过滤为包含该关键字的供应商；清空搜索应恢复全部列表。
5. 列表页下拉刷新，应重新拉取数据；无匹配时显示「没有找到相关供应商」。

---

## 3-4-supplier-edit-delete（循环完成日：2026-03-10）

**已验证**：否

1. 登录后进入供应商列表（如从更多/入口或直接访问 `/suppliers`），点击某条供应商应进入详情页（`/suppliers/:id`），页面显示该供应商的名称、联系人、电话、主营品类，且各字段可编辑。
2. 在详情页修改任一项后点击「保存」，应 Toast「保存成功」；将名称清空后点保存应提示「请填写供应商名称」。
3. 点击「删除供应商」应弹出确认弹窗「确定要删除该供应商吗？」；点取消应关闭弹窗且不删除；点确认后应删除该供应商、跳回供应商列表并 Toast「已删除」。

---

## 4-1-sale-order-create（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/sale-orders/new`，应被重定向到 `/login?redirect=/sale-orders/new`。
2. 登录后从首页进入「新建出货单」或直接访问 `/sale-orders/new`，页面应显示「选择客户」区域与「添加商品」按钮、底部合计与「保存」按钮。
3. 点击「选择客户」→ 弹窗内搜索或选择客户 → 选中后客户名称显示在页面头部。
4. 点击「添加商品」→ 弹窗内搜索或选择商品、输入数量 → 确认后条目出现在列表，单价为售价、小计与底部总金额正确；可多次添加多条商品。
5. 在列表中修改某条的数量或单价，或删除某条，小计与底部总金额应实时更新。
6. 未选择客户或未添加任何商品时点击「保存」，按钮应禁用或 Toast 提示「请选择客户」/「请至少添加一件商品」。
7. 选择客户并添加至少一件商品后点击「保存」，应 Toast「已保存」，表单可清空；Supabase 中 `sale_orders`、`sale_order_items` 应有对应草稿记录，`total_amount` 正确。

---

## 4-2-sale-order-confirm（循环完成日：2026-03-10）

**已验证**：否

1. 登录后：新建出货单 → 选客户、加商品、保存 → 应跳转到该单详情页。
2. 在详情页点击「确认出货」→ 二次确认 → 确认后应 Toast「出货单已确认」、页面刷新为已确认状态；Supabase 中应有对应 `receivables`、`stock_logs` 记录。
3. 若某商品库存不足再点确认，应看到「商品 XXX 库存不足（当前 N 件，需要 M 件）」提示。

---

## 4-3-sale-order-list-detail（循环完成日：2026-03-10）

**已验证**：否

1. 登录后进入出货单列表（从底部「单据」Tab 或直接访问 `/sale-orders`），应显示出货单列表，每条显示单号、客户名、总金额、日期、状态，按日期倒序；无数据时显示「暂无出货单」。
2. 列表页下拉刷新，应重新加载列表。
3. 使用筛选：按客户筛选后列表仅显示该客户的出货单；按日期范围筛选后列表在起止日期范围内；清除筛选后恢复全部列表。
4. 点击某条出货单应跳转至详情页 `/sale-orders/:id`，详情页显示单号、客户名称、所有商品条目（商品名、数量、单价、小计）、总金额、创建时间、状态。

---

## 4-4-photo-recognize-sale（循环完成日：2026-03-10）

**已验证**：否

1. **Edge Function 密钥**：在 Supabase Dashboard → Edge Functions → recognize-receipt → Secrets 中设置 `OPENAI_API_KEY`（OpenAI API Key），否则拍照识别会返回 500。
2. 登录后进入新建出货单页 `/sale-orders/new`，应看到「拍照识别」「添加商品」「保存」三个按钮。
3. 点击「拍照识别」→ 选择一张纸质出货单图片（或手机拍照）→ 应显示「正在识别...」遮罩；若 Edge Function 正常且图片清晰，识别成功后自动填充客户（匹配 customers 表）与商品条目（匹配 products 表）；未匹配到客户时 Toast「未匹配到客户「XXX」，请手动选择」；未匹配到的商品以「需手动选择：商品名」占位显示，可点击该行的「选择商品」按钮替换为实际商品。
4. 识别失败（模糊图、非单据图、或 API 错误）时，应 Toast「识别失败，请重新拍照或手动录入」，不覆盖当前已填表单。
5. 存在「需手动选择」占位行时点击「保存」，应 Toast「请为「需手动选择」的条目选择商品」；为所有占位行选择商品后，可正常保存草稿并继续确认出货流程。
6. （可选）在 Supabase Dashboard 中确认 Edge Function `recognize-receipt` 已部署且状态为 ACTIVE；本地可用 `supabase functions serve recognize-receipt` 配合 `.env` 中 OPENAI_API_KEY 做联调。

---

## 5-1-purchase-order-create（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/purchase-orders/new`，应被重定向到 `/login?redirect=/purchase-orders/new`。
2. 登录后从首页进入「新建进货单」或直接访问 `/purchase-orders/new`，页面应显示「选择供应商」区域与「添加商品」按钮、底部合计与「保存」按钮。
3. 点击「选择供应商」→ 弹窗内搜索或选择供应商 → 选中后供应商名称显示在页面头部。
4. 点击「添加商品」→ 弹窗内搜索或选择商品、输入数量与进价（进价可默认带出商品进价）→ 确认后条目出现在列表，小计与底部总金额正确；可多次添加多条商品。
5. 在列表中修改某条的数量或进价，或删除某条，小计与底部总金额应实时更新。
6. 未选择供应商或未添加任何商品时点击「保存」，按钮应禁用或 Toast 提示「请选择供应商」/「请至少添加一件商品」。
7. 选择供应商并添加至少一件商品后点击「保存」，应 Toast「已保存」并跳转到该进货单详情页（`/purchase-orders/:id`）；详情页显示单号、供应商名、总金额、状态（草稿）；Supabase 中 `purchase_orders`、`purchase_order_items` 应有对应草稿记录，`total_amount` 正确。

---

## 5-2-purchase-order-confirm（循环完成日：2026-03-10）

**已验证**：否

1. 登录后先创建一张草稿进货单（`/purchase-orders/new` 选供应商并添加商品后保存），应跳转到 `/purchase-orders/:id` 详情页，状态为「草稿」。
2. 在草稿详情页点击「确认进货」应弹出二次确认框，展示供应商、商品数、总金额；点击取消应不改状态。
3. 在确认框点击确认后，应 Toast「进货单已确认」，详情页刷新并显示状态「已确认」；按钮不再显示。
4. 在 Supabase 中核对：`purchase_orders.status` 变为 `confirmed`；`stock_logs` 新增 `reason='purchase_order'` 的明细记录；`payables` 新增一条 `purchase_order_id` 对应记录，`amount=订单 total_amount` 且 `status='unpaid'`。
5. （可选异常验证）网络断开或模拟数据库错误后点击确认，应提示「操作失败，请重试」，且订单状态、库存、应付记录不应出现部分成功（事务回滚）。

---

## 5-3-purchase-order-list-detail（循环完成日：2026-03-10）

**已验证**：否

1. 登录后从首页点击「进货单列表」或直接访问 `/purchase-orders`，应进入进货单列表页，标题为「进货单」；有数据时每条显示单号、供应商名、总金额、日期、状态，按日期倒序；无数据时显示「暂无进货单」及「点击「新建进货单」创建第一单」。
2. 列表页下拉刷新，应重新加载列表。
3. 点击「筛选」打开弹窗：可选择「供应商」（下拉选单）、「日期起」「日期止」；点击「确定」后列表仅显示符合条件的进货单；点击「清除」后恢复全部列表；有筛选条件时筛选按钮显示红点。
4. 点击某条进货单应跳转至详情页 `/purchase-orders/:id`，详情页显示单号、供应商名称、所有商品条目（商品名、数量、单价、小计）、总金额、创建时间、状态。

---

## 5-4-photo-recognize-purchase（循环完成日：2026-03-10）

**已验证**：否

1. **Edge Function**：复用 4.4 的 `recognize-receipt`，无需新建；若尚未配置，请在 Supabase Dashboard → Edge Functions → recognize-receipt → Secrets 中设置 `OPENAI_API_KEY`。
2. 登录后进入新建进货单页 `/purchase-orders/new`，应看到「拍照识别」「添加商品」「保存」三个按钮。
3. 点击「拍照识别」→ 选择一张纸质进货单图片（或手机拍照）→ 应显示「正在识别...」遮罩；若 Edge Function 正常且图片清晰，识别成功后自动填充供应商（匹配 suppliers 表）与商品条目（匹配 products 表）；未匹配到供应商时 Toast「未匹配到供应商「XXX」，请手动选择」；未匹配到的商品以「需手动选择：商品名」占位显示，可点击该行的「选择商品」按钮替换为实际商品。
4. 识别失败（模糊图、非单据图、或 API 错误）时，应 Toast「识别失败，请重新拍照或手动录入」，不覆盖当前已填表单。
5. 存在「需手动选择」占位行时点击「保存」，应 Toast「请为「需手动选择」的条目选择商品」；为所有占位行选择商品后，可正常保存草稿并继续确认进货流程。

---

## 6-1-receivables-list（循环完成日：2026-03-10）

**已验证**：否

1. 登录后从首页点击「查应收」或直接访问 `/receivables`，应进入应收账款页面；无应收记录时显示「暂无应收账款」。
2. 若有应收记录（需先创建出货单并确认），页面应显示按客户汇总的列表，每条显示客户名、总欠款金额（红色）、未付单据数量；按总欠款降序排列。
3. 点击某客户行应展开该客户的应收明细列表，每条显示：出货单号、金额、已付金额、状态（未付/部分付）、日期。
4. 下拉刷新后数据应重新加载。
5. 金额显示应保留两位小数。

---

## 6-2-payables-list（循环完成日：2026-03-10）

**已验证**：否

1. 登录后从首页点击「查应付」或直接访问 `/payables`，应进入应付账款页面；无应付记录时显示「暂无应付账款」。
2. 若有应付记录（需先创建进货单并确认），页面应显示按供应商汇总的列表，每条显示供应商名、总欠款金额（红色）、未付单据数量；按总欠款降序排列。
3. 点击某供应商行应展开该供应商的应付明细列表，每条显示：进货单号、金额、已付金额、状态（未付/部分付）、日期。
4. 下拉刷新后数据应重新加载。
5. 金额显示应保留两位小数。
6. 折叠关闭某供应商时，不应发起空 supplierId 的请求，控制台无报错。

---

## 6-3-payment-status（循环完成日：2026-03-10）

**已验证**：否

1. 登录后进入「查应收」或 `/receivables`，若有未付/部分付的应收记录，展开某客户后每条明细应显示「标记付款」按钮；已付（paid）记录不显示该按钮。
2. 点击某条应收的「标记付款」，应弹出底部面板，显示应收总额、已付金额、剩余未付金额，以及「本次付款金额」输入框和确认/取消按钮。
3. 输入本次付款金额等于剩余未付并确认，应 Toast「已更新」，该条状态变为「已付」，已付金额更新，列表与汇总刷新；输入金额小于剩余未付并确认，状态变为「部分付款」，已付金额累加。
4. 输入本次付款金额大于剩余未付并点击确认，应 Toast「付款金额不能超过未付金额」，不更新数据，弹窗保持打开。
5. 进入「查应付」或 `/payables`，对未付/部分付的应付记录执行上述 2–4 的同样操作，行为一致（应付总额、已付、剩余未付、标记付款后刷新）。

---

## 6-4-receivable-payable-detail（循环完成日：2026-03-10）

**已验证**：否

1. **应收 → 查看原始单据**：登录后进入「查应收」`/receivables`，展开某客户，在任一条应收明细上点击「查看原始单据」，应跳转到该笔应收关联的出货单详情页（`/sale-orders/:id`），详情内容与应收记录一致。
2. **应付 → 查看原始单据**：进入「查应付」`/payables`，展开某供应商，在任一条应付明细上点击「查看原始单据」，应跳转到该笔应付关联的进货单详情页（`/purchase-orders/:id`），详情内容与应付记录一致。
3. **客户详情页**：进入客户列表 `/customers`，点击某客户进入详情页（`/customers/:id`），页面底部应显示「应收账款汇总」区块（总欠款：¥xxx，若无欠款则显示 ¥0.00）和「历史出货单」区块；历史出货单列表显示该客户的出货单（单号、日期、金额、状态），最多 20 条；点击某条出货单应跳转到该出货单详情页。
4. **供应商详情页**：进入供应商列表 `/suppliers`，点击某供应商进入详情页（`/suppliers/:id`），页面底部应显示「应付账款汇总」区块（总欠款：¥xxx）和「历史进货单」区块；历史进货单列表显示该供应商的进货单（单号、日期、金额、状态），最多 20 条；点击某条进货单应跳转到该进货单详情页。

---

## 7-1-inventory-overview（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/stock`，应被重定向到 `/login?redirect=/stock`。
2. 登录后从「更多」页点击「库存总览」或直接访问 `/stock`，应进入库存总览页，顶部标题为「库存总览」；有商品数据时列表显示每条商品的名称、规格、当前库存数量（单位：件），按名称排序；无商品时显示「暂无商品数据」。
3. 在库存总览页搜索框输入商品名称或规格关键字（防抖约 300ms），列表应过滤为名称或规格包含该关键字的商品；清空搜索应恢复全部列表；无匹配时显示「没有找到相关商品」。
4. 列表页下拉刷新，应重新拉取数据。
5. 当某商品库存为 0 时，该行应以醒目样式显示：商品名红色、显示红色「库存不足」标签、库存数量红色；库存大于 0 的商品为正常样式。

---

## 7-2-inventory-adjust（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/stock`，应被重定向到 `/login?redirect=/stock`。
2. 登录后进入库存总览 `/stock`，点击某条商品行（或行内「调整库存」），应弹出「调整库存」弹窗，显示该商品名称、规格、当前库存数量，以及「调整后库存」「调整原因」输入项。
3. 不填写调整原因直接点击「确认」，应 Toast「请填写调整原因」，且不发起请求。
4. 填写调整原因，将「调整后库存」设为负数并点击「确认」，应 Toast 提示请输入有效库存（≥0），且不发起请求。
5. 填写调整原因与有效调整后库存（如 10），点击「确认」，应 Toast「调整成功」、弹窗关闭、列表刷新且该商品库存显示为新数量；在 Supabase 表 `products` 中该商品 `stock` 为新值，表 `stock_logs` 中新增一条记录：`reason = 'manual'`，`change` = 新数量 - 旧数量，`balance` = 新数量。

---

## 7-3-stock-logs（循环完成日：2026-03-10）

**已验证**：否

1. 未登录时访问 `/stock`，应被重定向到登录页。
2. 登录后进入库存总览 `/stock`，每行商品应显示「调整库存」与「变动记录」两个入口；点击「变动记录」（不触发整行打开调整弹窗），应弹出「变动记录」弹窗，标题含该商品名称。
3. 弹窗内应显示该商品的库存变动列表（来自 `stock_logs`），每条包含：变动时间（本地格式）、变动数量（正数 +N、负数 -N）、变动后余额、原因（出货/进货/手动）、单据号（有则显示，无则「—」）；若无记录则显示「暂无变动记录」。
4. 当某条变动原因为「出货」或「进货」且有关联单据时，单据号应为可点击链接；点击后应跳转到对应出货单详情页（`/sale-orders/:id`）或进货单详情页（`/purchase-orders/:id`）。
5. 原因为「手动」的变动记录，单据号显示「—」，不可点击跳转。

---

## 7-4-realtime-sync（循环完成日：2026-03-10）

**已验证**：否

1. **多端库存/列表自动刷新**：在浏览器 A 登录并打开库存总览 `/stock`（或应收 `/receivables`、出货单列表 `/sale-orders` 等）；在浏览器 B（或隐身窗口）用同账号或另一账号登录，执行会改数据的操作（如确认一笔出货单、在库存总览调整某商品库存、或标记一笔应收付款）。浏览器 A 中对应列表应在数秒内自动更新，无需手动下拉刷新。
2. **Realtime 订阅生效**：确认 products、sale_orders、purchase_orders、receivables、payables 任一表在 Supabase 中发生变更时，当前打开的相关页面（库存总览、出货单列表、进货单列表、应收、应付）会收到变更并刷新展示。
3. **可选：断线重连**：在 DevTools 中限流或短暂断网后恢复，确认 Realtime 自动重连且恢复后数据可再次拉取正确（列表可正常刷新）。

---

## 7-5-backup-restore（循环完成日：2026-03-10）

**已验证**：否

1. **应用内导出**：登录后进入「更多」页（底部 Tab「更多」），点击「导出数据」，应出现「正在导出…」Toast，随后触发浏览器下载 JSON 文件（文件名形如 `huotong-export-YYYYMMDDHHmmss.json`）；打开文件应包含 `exportedAt`、`products`、`customers`、`suppliers`、`sale_orders`、`sale_order_items`、`purchase_orders`、`purchase_order_items`、`receivables`、`payables`、`stock_logs` 等 key，且为当前用户可见的业务数据。
2. **开发者备份**（可选）：在项目根配置 `DATABASE_URL` 或先执行 `supabase link`，运行 `./scripts/backup-db.sh`，应在 `_backups/` 下生成 `dump_YYYYMMDD_HHmm.sql`；打开 `docs/backup-restore.md` 可按文档执行恢复（建议仅在测试环境验证）。

---

## 8-1-capacitor-android-init（循环完成日：2026-03-12）

**已验证**：否

1. 在 `huotong-app` 执行 `java -version`，确认当前 JDK 为 21 或更高版本；若仍是 Java 17/11/8，应先切换 `JAVA_HOME` 再继续后续步骤。
2. 执行 `npm run android:sync`，应成功完成 build + cap sync，且 `android/app/src/main/assets/public` 下可见最新前端资源文件。
3. 执行 `npm run android:build`，应成功完成 `assembleDebug`，证明当前环境可以产出 Android 调试包。
4. 执行 `npm run android:open`，Android Studio 能正常打开 `android/` 工程并完成 Gradle 同步。
5. 连接一台 Android 真机（或启动模拟器）后执行 `npm run android:run`，应用应可安装并从桌面图标启动，进入 HuoTong 登录页或首页。
6. 启动后确认页面加载来源为本地打包资源（无浏览器地址栏、无跳转公网 H5 入口）；基本路由可正常进入（如登录页与首页）。
7. 若第 5 步失败，记录机型、Android 版本、是否有可用设备/模拟器、错误日志摘要，便于 Story 8.2 继续处理应用身份与构建配置。

---

## 8-2-android-app-identity-build-config（循环完成日：2026-03-12）

**已验证**：否

1. 在 `huotong-app` 执行 `java -version`，确认当前 JDK 为 21 或更高版本；若不足 21，请先切换 `JAVA_HOME`。
2. 在 Android 真机或模拟器安装最新 debug 包（可先执行 `npm run android:build` + Android Studio 安装），应用图标应为新的「橙色底 + 白色几何 H」样式，应用名称显示为「货通」。
3. 首次启动时应看到新的启动页样式（橙色主视觉），随后正常进入登录页或首页。
4. 在 `huotong-app/android` 下复制并填写签名配置：`cp keystore.properties.example keystore.properties`，并确保 `storeFile` 指向真实 keystore 文件。
5. 执行 `npm run android:build:release`，应通过签名校验并成功产出 release APK（`android/app/build/outputs/apk/release/`）。
6. 用已安装旧版本的设备执行一次覆盖安装：新 APK 可覆盖旧包安装，应用可正常启动，登录态和核心页面访问无异常。
7. 完成后将本条「已验证」改为「是」，并记录机型、Android 版本、APK 文件名（含 versionName/versionCode）用于追踪。

---

## 8-2-android-app-identity-build-config（循环完成日：2026-03-12，收口复验）

**已验证**：是

1. 在 `huotong-app` 执行 `export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home && export PATH=$JAVA_HOME/bin:$PATH && java -version`，应显示 `21.x`。
2. 执行 `npm run android:build`，应成功产出 `android/app/build/outputs/apk/debug/app-debug.apk`。
3. 执行 `npm run android:build:release`，应通过签名预检并成功产出 `android/app/build/outputs/apk/release/app-release.apk`。
4. 在 Android 真机或模拟器安装 debug 包，确认应用名称显示为「货通」，桌面图标为「橙色底 + 白色几何 H」。
5. 首次启动应展示橙色主视觉启动页，然后进入登录页/首页；核心页面（登录、首页）可正常打开。
6. 在已安装旧版本的设备执行覆盖安装（release APK），确认可覆盖升级，且登录态与核心页面访问无异常。
7. 验证完成后将本条「已验证」改为「是」，并补充机型、Android 版本、APK 文件名（`app-debug.apk` / `app-release.apk`）与验证日期。

---

## 9-1-session-resume-lifecycle-adaptation（循环完成日：2026-03-12）

**已验证**：否

1. Android 真机安装最新包并登录后，先打开任一关键列表页（如 `/products` 或 `/sale-orders`），按 Home 键切到后台 5 秒后回到 App：应保持登录状态，不应无提示跳到登录页。
2. 在另一台设备或 Supabase 后台让当前登录会话失效（例如手动登出该用户或删除会话）后，再把 App 从后台切回前台：应出现提示「登录状态已失效，请重新登录」，并跳转到 `/login`。
3. 在关键列表页（商品/客户/供应商/出货单/进货单/应收/应付/库存）各执行一次“切后台→回前台”，页面应自动刷新；若刷新失败应有中文失败提示，且仍可通过下拉刷新手动重试成功。
4. 在 `VITE_REALTIME_ENABLED=false`（或关闭 Realtime）的情况下重复第 3 步，恢复后仍应能看到最新数据（证明不依赖 Realtime）。

---

## 9-2-android-back-button-navigation（循环完成日：2026-03-12）

**已验证**：否

1. 在 Android 真机安装最新包并登录后，进入任一二级页面（如从 `/sale-orders` 点进 `/sale-orders/:id`），按系统返回键：应回到上一级页面，而不是直接退出应用。
2. 在以下任一页面先打开底部弹层（如出货/进货列表筛选弹层、库存调整弹层、应收/应付「标记付款」弹层），按系统返回键：应优先关闭当前弹层，页面路由不变化。
3. 在首页 `/`（或登录页 `/login`）按系统返回键：应弹出中文退出确认弹窗；点「取消」保持在当前页，点「退出」后应用退出。
4. 在一级但非首页页面（如 `/products`、`/more`）按返回键：若存在路由历史应回退；若无历史应回到首页，再次返回触发退出确认。
5. 执行完第 1～4 步后，再验证关键业务入口（商品、单据、库存、应收、应付）能正常打开，确认新增返回键逻辑未影响基础导航。

---

## 10-1-network-error-feedback-retry（循环完成日：2026-03-12）

**已验证**：否

1. 在 Android 真机（或浏览器 DevTools 断网）打开登录页 `/login`，输入正确账号后点击「登录」：在断网场景应出现“登录失败，请检查网络后重试”提示，并弹出“重试登录”入口；恢复网络后点击“重试登录”应可成功进入首页。
2. 登录后进入商品列表 `/products`，断网并刷新页面：应出现“网络异常，加载商品失败”提示区域，且可见「重试加载」按钮；恢复网络后点击「重试加载」应成功恢复列表数据。
3. 进入商品编辑页 `/products/:id`（或新增页 `/products/new`）后断网点击「保存」：应提示“保存失败，请检查网络后重试”，并出现“重试保存”入口；恢复网络后点击“重试保存”应保存成功并返回商品列表。
4. 进入出货单详情 `/sale-orders/:id`（草稿单）后断网点击「确认出货」：应出现“确认出货失败，请检查网络后重试”的重试入口；恢复网络后点击“重试确认”应提示“出货单已确认”并刷新为已确认状态。
5. 进入进货单详情 `/purchase-orders/:id`（草稿单）后断网点击「确认进货」：应出现“确认进货失败，请检查网络后重试”的重试入口；恢复网络后点击“重试确认”应提示“进货单已确认”并刷新为已确认状态。

---

## 10-2-android-first-release-degrade-entry-shaping（循环完成日：2026-03-12）

**已验证**：否

1. 在 Android 真机安装最新包并登录，进入新建出货单 `/sale-orders/new` 与新建进货单 `/purchase-orders/new`：页面不应显示「拍照识别」按钮，应显示“Android 首版暂不开放拍照识别，请先手动录入”提示，且「添加商品」「保存」可正常使用。
2. 在 Android 端进入「更多」页 `/more`：应显示「库存总览」入口；导出入口应为“导出数据（暂不开放）”并带有说明文案，不应触发浏览器下载行为。
3. 在 Android 端进入商品列表 `/products`：不应显示「批量录入」按钮；若手动访问 `/products/batch`，应 Toast 提示“Android 首版暂不开放批量录入，请使用新增商品逐条录入”，并自动跳回 `/products`。
4. 在 Android 端验证核心链路：登录、商品新增、客户/供应商管理、新建并保存进货/出货单、应收应付页面、库存页面都可正常打开和操作（说明降级未影响核心流程）。
5. 在 Web 浏览器（非原生 Android 容器）验证同版本代码：`/products` 桌面宽屏仍可看到「批量录入」入口，「更多」页仍可执行导出（用于确认“仅 Android 降级”生效）。

---

## 11-1-version-display-overwrite-upgrade-verification（循环完成日：2026-03-12）

**已验证**：否

1. 在 Android 设备上安装旧版本 APK（如 `versionCode=1 / versionName=1.0.0`），登录后确认可进入首页与「更多」页。
2. 不卸载旧版本，直接安装新版本 APK（当前应为 `versionCode=2 / versionName=1.0.1`），确认可覆盖安装成功（无签名冲突、无包名冲突）。
3. 打开 App 的「更多」页（`/more`），确认出现「当前版本」字段，且值显示为当前安装版本（如 `1.0.1 (build 2)`）。
4. 覆盖升级后验证登录态仍可用：无需重新登录即可访问首页与至少一个业务页（如 `/products`）。
5. 覆盖升级后验证核心数据可访问：商品列表或单据列表能正常加载，无“数据异常丢失”现象。
6. 如覆盖安装失败，请先核对：`applicationId` 是否仍为 `com.huotong.app`、签名是否使用同一 keystore、`versionCode` 是否递增。

